using UnityEngine;
using UnityEngine.UI;

public class UIManager : MonoBehaviour
{
    public Text statsText;

    private void Update()
    {
        //statsText.text = "Jumps Used: " + StatManager.Instance.totalJumpsUsed + "\nLevels Completed: " + StatManager.Instance.levelsCompleted;
        
    }
}